openapi: 3.0.0
servers:
- url: https://management.azure.com/
info:
  title: Application Client
  description: Application Client.
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  version: 2019-07-01-stackql-generated
  x-serviceAlias: resource_managedapplications
  x-sdkPackage: azure-mgmt-resource-managedapplications
  x-plane: mgmt
security:
- azure_auth:
  - user_impersonation
components:
  securitySchemes:
    azure_auth:
      description: Azure Active Directory OAuth2 Flow.
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/authorize
          scopes:
            user_impersonation: impersonate your user account
  schemas:
    Sku:
      type: object
      description: SKU for the resource. All required parameters must be populated in order to send to server.
      properties:
        name:
          type: string
          description: The SKU name. Required.
        tier:
          type: string
          description: The SKU tier.
        size:
          type: string
          description: The SKU size.
        family:
          type: string
          description: The SKU family.
        model:
          type: string
          description: The SKU model.
        capacity:
          type: integer
          description: The SKU capacity.
      required:
      - name
    ApplicationAuthorization:
      type: object
      description: The managed application provider authorization. All required parameters must be populated in
        order to send to server.
      properties:
        principalId:
          type: string
          description: The provider's principal identifier. This is the identity that the provider will use to call
            ARM to manage the managed application resources. Required.
        roleDefinitionId:
          type: string
          description: The provider's role definition identifier. This role will define all the permissions that
            the provider must have on the managed application's container resource group. This role definition cannot
            have permission to delete the resource group. Required.
      required:
      - principalId
      - roleDefinitionId
    ApplicationDefinitionArtifact:
      type: object
      description: Application definition artifact. All required parameters must be populated in order to send to
        server.
      properties:
        name:
          type: string
          description: 'The managed application definition artifact name. Required. Known values are: "NotSpecified",
            "ApplicationResourceTemplate", "CreateUiDefinition", and "MainTemplateParameters".'
        uri:
          type: string
          description: The managed application definition artifact blob uri. Required.
        type:
          type: string
          description: 'The managed application definition artifact type. Required. Known values are: "NotSpecified",
            "Template", and "Custom".'
      required:
      - name
      - uri
      - type
    ApplicationNotificationPolicy:
      type: object
      description: Managed application notification policy. All required parameters must be populated in order to
        send to server.
      properties:
        notificationEndpoints:
          type: array
          items:
            $ref: '#/components/schemas/ApplicationNotificationEndpoint'
          description: The managed application notification endpoint. Required.
      required:
      - notificationEndpoints
    ApplicationNotificationEndpoint:
      type: object
      description: Managed application notification endpoint. All required parameters must be populated in order
        to send to server.
      properties:
        uri:
          type: string
          description: The managed application notification endpoint uri. Required.
      required:
      - uri
    ApplicationPackageLockingPolicyDefinition:
      type: object
      description: Managed application locking policy.
      properties:
        allowedActions:
          type: array
          items:
            type: string
          description: The deny assignment excluded actions.
        allowedDataActions:
          type: array
          items:
            type: string
          description: The deny assignment excluded data actions.
    ApplicationDeploymentPolicy:
      type: object
      description: Managed application deployment policy. All required parameters must be populated in order to
        send to server.
      properties:
        deploymentMode:
          type: string
          description: 'The managed application deployment mode. Required. Known values are: "NotSpecified", "Incremental",
            and "Complete".'
      required:
      - deploymentMode
    ApplicationManagementPolicy:
      type: object
      description: Managed application management policy.
      properties:
        mode:
          type: string
          description: 'The managed application management mode. Known values are: "NotSpecified", "Unmanaged",
            and "Managed".'
    ApplicationPolicy:
      type: object
      description: Managed application policy.
      properties:
        name:
          type: string
          description: The policy name.
        policyDefinitionId:
          type: string
          description: The policy definition Id.
        parameters:
          type: string
          description: The policy parameters.
    ApplicationDefinitionFlattened:
      type: object
      description: Flattened view of ApplicationDefinition ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        location:
          type: string
          description: Resource location.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        managedBy:
          type: string
          description: ID of the resource that manages this resource.
        sku:
          $ref: '#/components/schemas/Sku'
          description: The SKU of the resource.
        lockLevel:
          type: string
          description: 'The managed application lock level. Required. Known values are: "CanNotDelete", "ReadOnly",
            and "None".'
        displayName:
          type: string
          description: The managed application definition display name.
        isEnabled:
          type: boolean
          description: A value indicating whether the package is enabled or not.
        authorizations:
          type: array
          items:
            $ref: '#/components/schemas/ApplicationAuthorization'
          description: The managed application provider authorizations.
        artifacts:
          type: array
          items:
            $ref: '#/components/schemas/ApplicationDefinitionArtifact'
          description: The collection of managed application artifacts. The portal will use the files specified
            as artifacts to construct the user experience of creating a managed application from a managed application
            definition.
        description:
          type: string
          description: The managed application definition description.
        packageFileUri:
          type: string
          description: The managed application definition package file Uri. Use this element.
        mainTemplate:
          type: object
          properties: {}
          description: The inline main template json which has resources to be provisioned. It can be a JObject
            or well-formed JSON string.
        createUiDefinition:
          type: object
          properties: {}
          description: The createUiDefinition json for the backing template with Microsoft.Solutions/applications
            resource. It can be a JObject or well-formed JSON string.
        notificationPolicy:
          $ref: '#/components/schemas/ApplicationNotificationPolicy'
          description: The managed application notification policy.
        lockingPolicy:
          $ref: '#/components/schemas/ApplicationPackageLockingPolicyDefinition'
          description: The managed application locking policy.
        deploymentPolicy:
          $ref: '#/components/schemas/ApplicationDeploymentPolicy'
          description: The managed application deployment policy.
        managementPolicy:
          $ref: '#/components/schemas/ApplicationManagementPolicy'
          description: The managed application management policy that determines publisher's access to the managed
            resource group.
        policies:
          type: array
          items:
            $ref: '#/components/schemas/ApplicationPolicy'
          description: The managed application provider policies.
    PagedApplicationDefinitionFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: '#/components/schemas/ApplicationDefinitionFlattened'
        nextLink:
          type: string
    ApplicationDefinition:
      type: object
      description: Information about managed application definition. Variables are only populated by the server,
        and will be ignored when sending a request. All required parameters must be populated in order to send to
        server.
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        location:
          type: string
          description: Resource location.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        managedBy:
          type: string
          description: ID of the resource that manages this resource.
        sku:
          $ref: '#/components/schemas/Sku'
          description: The SKU of the resource.
        properties:
          type: object
          properties:
            lockLevel:
              type: string
              description: 'The managed application lock level. Required. Known values are: "CanNotDelete", "ReadOnly",
                and "None".'
            displayName:
              type: string
              description: The managed application definition display name.
            isEnabled:
              type: boolean
              description: A value indicating whether the package is enabled or not.
            authorizations:
              type: array
              items:
                $ref: '#/components/schemas/ApplicationAuthorization'
              description: The managed application provider authorizations.
            artifacts:
              type: array
              items:
                $ref: '#/components/schemas/ApplicationDefinitionArtifact'
              description: The collection of managed application artifacts. The portal will use the files specified
                as artifacts to construct the user experience of creating a managed application from a managed application
                definition.
            description:
              type: string
              description: The managed application definition description.
            packageFileUri:
              type: string
              description: The managed application definition package file Uri. Use this element.
            mainTemplate:
              type: object
              properties: {}
              description: The inline main template json which has resources to be provisioned. It can be a JObject
                or well-formed JSON string.
            createUiDefinition:
              type: object
              properties: {}
              description: The createUiDefinition json for the backing template with Microsoft.Solutions/applications
                resource. It can be a JObject or well-formed JSON string.
            notificationPolicy:
              $ref: '#/components/schemas/ApplicationNotificationPolicy'
              description: The managed application notification policy.
            lockingPolicy:
              $ref: '#/components/schemas/ApplicationPackageLockingPolicyDefinition'
              description: The managed application locking policy.
            deploymentPolicy:
              $ref: '#/components/schemas/ApplicationDeploymentPolicy'
              description: The managed application deployment policy.
            managementPolicy:
              $ref: '#/components/schemas/ApplicationManagementPolicy'
              description: The managed application management policy that determines publisher's access to the managed
                resource group.
            policies:
              type: array
              items:
                $ref: '#/components/schemas/ApplicationPolicy'
              description: The managed application provider policies.
      required:
      - properties
    Plan:
      type: object
      description: Plan for the managed application. All required parameters must be populated in order to send
        to server.
      properties:
        name:
          type: string
          description: The plan name. Required.
        publisher:
          type: string
          description: The publisher ID. Required.
        product:
          type: string
          description: The product code. Required.
        promotionCode:
          type: string
          description: The promotion code.
        version:
          type: string
          description: The plan's version. Required.
      required:
      - name
      - publisher
      - product
      - version
    Identity:
      type: object
      description: Identity for the resource. Variables are only populated by the server, and will be ignored when
        sending a request.
      properties:
        principalId:
          type: string
          description: The principal ID of resource identity.
        tenantId:
          type: string
          description: The tenant ID of resource.
        type:
          type: string
          description: 'The identity type. Known values are: "SystemAssigned", "UserAssigned", "SystemAssigned,
            UserAssigned", and "None".'
        userAssignedIdentities:
          type: object
          properties: {}
          description: 'The list of user identities associated with the resource. The user identity dictionary key
            references will be resource ids in the form: ''/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identityName}''.'
    ApplicationBillingDetailsDefinition:
      type: object
      description: Managed application billing details definition.
      properties:
        resourceUsageId:
          type: string
          description: The managed application resource usage Id.
    ApplicationJitAccessPolicy:
      type: object
      description: Managed application Jit access policy. All required parameters must be populated in order to
        send to server.
      properties:
        jitAccessEnabled:
          type: boolean
          description: Whether the JIT access is enabled. Required.
        jitApprovalMode:
          type: string
          description: 'JIT approval mode. Known values are: "NotSpecified", "AutoApprove", and "ManualApprove".'
        jitApprovers:
          type: array
          items:
            $ref: '#/components/schemas/JitApproverDefinition'
          description: The JIT approvers.
        maximumJitAccessDuration:
          type: string
          description: The maximum duration JIT access is granted. This is an ISO8601 time period value.
      required:
      - jitAccessEnabled
    JitApproverDefinition:
      type: object
      description: JIT approver definition. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: The approver service principal Id. Required.
        type:
          type: string
          description: 'The approver type. Known values are: "user" and "group".'
        displayName:
          type: string
          description: The approver display name.
      required:
      - id
    ApplicationPackageContact:
      type: object
      description: The application package contact information. All required parameters must be populated in order
        to send to server.
      properties:
        contactName:
          type: string
          description: The contact name.
        email:
          type: string
          description: The contact email. Required.
        phone:
          type: string
          description: The contact phone number. Required.
      required:
      - email
      - phone
    ApplicationPackageSupportUrls:
      type: object
      description: The appliance package support URLs.
      properties:
        publicAzure:
          type: string
          description: The public azure support URL.
        governmentCloud:
          type: string
          description: The government cloud support URL.
    ApplicationArtifact:
      type: object
      description: Managed application artifact. All required parameters must be populated in order to send to server.
      properties:
        name:
          type: string
          description: 'The managed application artifact name. Required. Known values are: "NotSpecified", "ViewDefinition",
            "Authorizations", and "CustomRoleDefinition".'
        uri:
          type: string
          description: The managed application artifact blob uri. Required.
        type:
          type: string
          description: 'The managed application artifact type. Required. Known values are: "NotSpecified", "Template",
            and "Custom".'
      required:
      - name
      - uri
      - type
    ApplicationClientDetails:
      type: object
      description: The application client details to track the entity creating/updating the managed app resource.
      properties:
        oid:
          type: string
          description: The client Oid.
        puid:
          type: string
          description: The client Puid.
        applicationId:
          type: string
          description: The client application Id.
    ApplicationFlattened:
      type: object
      description: Flattened view of Application ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        location:
          type: string
          description: Resource location.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        managedBy:
          type: string
          description: ID of the resource that manages this resource.
        sku:
          $ref: '#/components/schemas/Sku'
          description: The SKU of the resource.
        plan:
          $ref: '#/components/schemas/Plan'
          description: The plan information.
        kind:
          type: string
          description: The kind of the managed application. Allowed values are MarketPlace and ServiceCatalog. Required.
        identity:
          $ref: '#/components/schemas/Identity'
          description: The identity of the resource.
        managedResourceGroupId:
          type: string
          description: The managed resource group Id.
        applicationDefinitionId:
          type: string
          description: The fully qualified path of managed application definition Id.
        parameters:
          type: object
          properties: {}
          description: Name and value pairs that define the managed application parameters. It can be a JObject
            or a well formed JSON string.
        outputs:
          type: object
          properties: {}
          description: Name and value pairs that define the managed application outputs.
        provisioningState:
          type: string
          description: 'The managed application provisioning state. Known values are: "NotSpecified", "Accepted",
            "Running", "Ready", "Creating", "Created", "Deleting", "Deleted", "Canceled", "Failed", "Succeeded",
            and "Updating".'
        billingDetails:
          $ref: '#/components/schemas/ApplicationBillingDetailsDefinition'
          description: The managed application billing details.
        jitAccessPolicy:
          $ref: '#/components/schemas/ApplicationJitAccessPolicy'
          description: The managed application Jit access policy.
        publisherTenantId:
          type: string
          description: The publisher tenant Id.
        authorizations:
          type: array
          items:
            $ref: '#/components/schemas/ApplicationAuthorization'
          description: The read-only authorizations property that is retrieved from the application package.
        managementMode:
          type: string
          description: 'The managed application management mode. Known values are: "NotSpecified", "Unmanaged",
            and "Managed".'
        customerSupport:
          $ref: '#/components/schemas/ApplicationPackageContact'
          description: The read-only customer support property that is retrieved from the application package.
        supportUrls:
          $ref: '#/components/schemas/ApplicationPackageSupportUrls'
          description: The read-only support URLs property that is retrieved from the application package.
        artifacts:
          type: array
          items:
            $ref: '#/components/schemas/ApplicationArtifact'
          description: The collection of managed application artifacts.
        createdBy:
          $ref: '#/components/schemas/ApplicationClientDetails'
          description: The client entity that created the JIT request.
        updatedBy:
          $ref: '#/components/schemas/ApplicationClientDetails'
          description: The client entity that last updated the JIT request.
    PagedApplicationFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: '#/components/schemas/ApplicationFlattened'
        nextLink:
          type: string
    Application:
      type: object
      description: Information about managed application. Variables are only populated by the server, and will be
        ignored when sending a request. All required parameters must be populated in order to send to server.
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        location:
          type: string
          description: Resource location.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        managedBy:
          type: string
          description: ID of the resource that manages this resource.
        sku:
          $ref: '#/components/schemas/Sku'
          description: The SKU of the resource.
        plan:
          $ref: '#/components/schemas/Plan'
          description: The plan information.
        kind:
          type: string
          description: The kind of the managed application. Allowed values are MarketPlace and ServiceCatalog. Required.
        identity:
          $ref: '#/components/schemas/Identity'
          description: The identity of the resource.
        properties:
          type: object
          properties:
            managedResourceGroupId:
              type: string
              description: The managed resource group Id.
            applicationDefinitionId:
              type: string
              description: The fully qualified path of managed application definition Id.
            parameters:
              type: object
              properties: {}
              description: Name and value pairs that define the managed application parameters. It can be a JObject
                or a well formed JSON string.
            outputs:
              type: object
              properties: {}
              description: Name and value pairs that define the managed application outputs.
            provisioningState:
              type: string
              description: 'The managed application provisioning state. Known values are: "NotSpecified", "Accepted",
                "Running", "Ready", "Creating", "Created", "Deleting", "Deleted", "Canceled", "Failed", "Succeeded",
                and "Updating".'
            billingDetails:
              $ref: '#/components/schemas/ApplicationBillingDetailsDefinition'
              description: The managed application billing details.
            jitAccessPolicy:
              $ref: '#/components/schemas/ApplicationJitAccessPolicy'
              description: The managed application Jit access policy.
            publisherTenantId:
              type: string
              description: The publisher tenant Id.
            authorizations:
              type: array
              items:
                $ref: '#/components/schemas/ApplicationAuthorization'
              description: The read-only authorizations property that is retrieved from the application package.
            managementMode:
              type: string
              description: 'The managed application management mode. Known values are: "NotSpecified", "Unmanaged",
                and "Managed".'
            customerSupport:
              $ref: '#/components/schemas/ApplicationPackageContact'
              description: The read-only customer support property that is retrieved from the application package.
            supportUrls:
              $ref: '#/components/schemas/ApplicationPackageSupportUrls'
              description: The read-only support URLs property that is retrieved from the application package.
            artifacts:
              type: array
              items:
                $ref: '#/components/schemas/ApplicationArtifact'
              description: The collection of managed application artifacts.
            createdBy:
              $ref: '#/components/schemas/ApplicationClientDetails'
              description: The client entity that created the JIT request.
            updatedBy:
              $ref: '#/components/schemas/ApplicationClientDetails'
              description: The client entity that last updated the JIT request.
      required:
      - kind
    PlanPatchable:
      type: object
      description: Plan for the managed application.
      properties:
        name:
          type: string
          description: The plan name.
        publisher:
          type: string
          description: The publisher ID.
        product:
          type: string
          description: The product code.
        promotionCode:
          type: string
          description: The promotion code.
        version:
          type: string
          description: The plan's version.
    JitAuthorizationPolicies:
      type: object
      description: The JIT authorization policies. All required parameters must be populated in order to send to
        server.
      properties:
        principalId:
          type: string
          description: The the principal id that will be granted JIT access. Required.
        roleDefinitionId:
          type: string
          description: The role definition id that will be granted to the Principal. Required.
      required:
      - principalId
      - roleDefinitionId
    JitSchedulingPolicy:
      type: object
      description: The JIT scheduling policies. Variables are only populated by the server, and will be ignored
        when sending a request. All required parameters must be populated in order to send to server.
      properties:
        type:
          type: string
          description: 'The type of JIT schedule. Required. Known values are: "NotSpecified", "Once", and "Recurring".'
        duration:
          type: string
          description: The required duration of the JIT request. Required.
        startTime:
          type: string
          format: date-time
          description: The start time of the request. Required.
      required:
      - duration
      - startTime
    JitRequestDefinitionFlattened:
      type: object
      description: Flattened view of JitRequestDefinition ('properties' hoisted to the top level).
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        location:
          type: string
          description: Resource location.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        applicationResourceId:
          type: string
          description: The parent application id.
        publisherTenantId:
          type: string
          description: The publisher tenant id.
        jitAuthorizationPolicies:
          type: array
          items:
            $ref: '#/components/schemas/JitAuthorizationPolicies'
          description: The JIT authorization policies.
        jitSchedulingPolicy:
          $ref: '#/components/schemas/JitSchedulingPolicy'
          description: The JIT request properties.
        provisioningState:
          type: string
          description: 'The JIT request provisioning state. Known values are: "NotSpecified", "Accepted", "Running",
            "Ready", "Creating", "Created", "Deleting", "Deleted", "Canceled", "Failed", "Succeeded", and "Updating".'
        jitRequestState:
          type: string
          description: 'The JIT request state. Known values are: "NotSpecified", "Pending", "Approved", "Denied",
            "Failed", "Canceled", "Expired", and "Timeout".'
        createdBy:
          $ref: '#/components/schemas/ApplicationClientDetails'
          description: The client entity that created the JIT request.
        updatedBy:
          $ref: '#/components/schemas/ApplicationClientDetails'
          description: The client entity that last updated the JIT request.
    JitRequestDefinition:
      type: object
      description: Information about JIT request definition. Variables are only populated by the server, and will
        be ignored when sending a request.
      properties:
        id:
          type: string
          description: Resource ID.
        name:
          type: string
          description: Resource name.
        type:
          type: string
          description: Resource type.
        location:
          type: string
          description: Resource location.
        tags:
          type: object
          properties: {}
          description: Resource tags.
        properties:
          type: object
          properties:
            applicationResourceId:
              type: string
              description: The parent application id.
            publisherTenantId:
              type: string
              description: The publisher tenant id.
            jitAuthorizationPolicies:
              type: array
              items:
                $ref: '#/components/schemas/JitAuthorizationPolicies'
              description: The JIT authorization policies.
            jitSchedulingPolicy:
              $ref: '#/components/schemas/JitSchedulingPolicy'
              description: The JIT request properties.
            provisioningState:
              type: string
              description: 'The JIT request provisioning state. Known values are: "NotSpecified", "Accepted", "Running",
                "Ready", "Creating", "Created", "Deleting", "Deleted", "Canceled", "Failed", "Succeeded", and "Updating".'
            jitRequestState:
              type: string
              description: 'The JIT request state. Known values are: "NotSpecified", "Pending", "Approved", "Denied",
                "Failed", "Canceled", "Expired", and "Timeout".'
            createdBy:
              $ref: '#/components/schemas/ApplicationClientDetails'
              description: The client entity that created the JIT request.
            updatedBy:
              $ref: '#/components/schemas/ApplicationClientDetails'
              description: The client entity that last updated the JIT request.
    JitRequestDefinitionListResultFlattened:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: '#/components/schemas/JitRequestDefinitionFlattened'
        nextLink:
          type: string
    OperationDisplay:
      type: object
      description: Localized display information for this particular operation. Variables are only populated by
        the server, and will be ignored when sending a request.
      properties:
        provider:
          type: string
          description: The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights"
            or "Microsoft Compute".
        resource:
          type: string
          description: The localized friendly name of the resource type related to this operation. E.g. "Virtual
            Machines" or "Job Schedule Collections".
        operation:
          type: string
          description: The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create
            or Update Virtual Machine", "Restart Virtual Machine".
        description:
          type: string
          description: The short, localized friendly description of the operation; suitable for tool tips and detailed
            views.
    Operation:
      type: object
      description: Details of a REST API operation, returned from the Resource Provider Operations API. Variables
        are only populated by the server, and will be ignored when sending a request.
      properties:
        name:
          type: string
          description: 'The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write",
            "Microsoft.Compute/virtualMachines/capture/action".'
        isDataAction:
          type: boolean
          description: Whether the operation applies to data-plane. This is "true" for data-plane operations and
            "false" for ARM/control-plane operations.
        display:
          $ref: '#/components/schemas/OperationDisplay'
          description: Localized display information for this particular operation.
        origin:
          type: string
          description: 'The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit
            logs UX. Default value is "user,system". Known values are: "user", "system", and "user,system".'
        actionType:
          type: string
          description: Enum. Indicates the action type. "Internal" refers to actions that are for internal only
            APIs. "Internal"
    PagedOperation:
      type: object
      properties:
        value:
          type: array
          items:
            $ref: '#/components/schemas/Operation'
        nextLink:
          type: string
paths:
  ? /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/applicationDefinitions/{application_definition_name}?api-version=2019-07-01
  : get:
      operationId: ApplicationDefinitions_get
      description: Gets the managed application definition.
      parameters:
      - name: resource_group_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the resource group. The name is case insensitive. Required.
      - name: application_definition_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the managed application definition. Required.
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApplicationDefinitionFlattened'
      x-stackql-resource: application_definitions
      x-stackql-method: get
      x-stackql-verb: SELECT
      x-stackql-flatten: singleton
    put:
      operationId: ApplicationDefinitions_begin_create_or_update
      description: Creates a new managed application definition.
      parameters:
      - name: resource_group_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the resource group. The name is case insensitive. Required.
      - name: application_definition_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the managed application definition. Required.
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Resource location.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                managedBy:
                  type: string
                  description: ID of the resource that manages this resource.
                sku:
                  $ref: '#/components/schemas/Sku'
                  description: The SKU of the resource.
                properties:
                  type: object
                  properties:
                    lockLevel:
                      type: string
                      description: 'The managed application lock level. Required. Known values are: "CanNotDelete",
                        "ReadOnly", and "None".'
                    displayName:
                      type: string
                      description: The managed application definition display name.
                    isEnabled:
                      type: boolean
                      description: A value indicating whether the package is enabled or not.
                    authorizations:
                      type: array
                      items:
                        $ref: '#/components/schemas/ApplicationAuthorization'
                      description: The managed application provider authorizations.
                    artifacts:
                      type: array
                      items:
                        $ref: '#/components/schemas/ApplicationDefinitionArtifact'
                      description: The collection of managed application artifacts. The portal will use the files
                        specified as artifacts to construct the user experience of creating a managed application
                        from a managed application definition.
                    description:
                      type: string
                      description: The managed application definition description.
                    packageFileUri:
                      type: string
                      description: The managed application definition package file Uri. Use this element.
                    mainTemplate:
                      type: object
                      properties: {}
                      description: The inline main template json which has resources to be provisioned. It can be
                        a JObject or well-formed JSON string.
                    createUiDefinition:
                      type: object
                      properties: {}
                      description: The createUiDefinition json for the backing template with Microsoft.Solutions/applications
                        resource. It can be a JObject or well-formed JSON string.
                    notificationPolicy:
                      $ref: '#/components/schemas/ApplicationNotificationPolicy'
                      description: The managed application notification policy.
                    lockingPolicy:
                      $ref: '#/components/schemas/ApplicationPackageLockingPolicyDefinition'
                      description: The managed application locking policy.
                    deploymentPolicy:
                      $ref: '#/components/schemas/ApplicationDeploymentPolicy'
                      description: The managed application deployment policy.
                    managementPolicy:
                      $ref: '#/components/schemas/ApplicationManagementPolicy'
                      description: The managed application management policy that determines publisher's access
                        to the managed resource group.
                    policies:
                      type: array
                      items:
                        $ref: '#/components/schemas/ApplicationPolicy'
                      description: The managed application provider policies.
              required:
              - properties
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApplicationDefinition'
      x-stackql-resource: application_definitions
      x-stackql-method: create_or_update
      x-stackql-verb: INSERT
    delete:
      operationId: ApplicationDefinitions_begin_delete
      description: Deletes the managed application definition.
      parameters:
      - name: resource_group_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the resource group. The name is case insensitive. Required.
      - name: application_definition_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the managed application definition to delete. Required.
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Success (no content / accepted)
      x-stackql-resource: application_definitions
      x-stackql-method: delete
      x-stackql-verb: DELETE
  ? /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/applicationDefinitions?api-version=2019-07-01
  : get:
      operationId: ApplicationDefinitions_list_by_resource_group
      description: Lists the managed application definitions in a resource group.
      parameters:
      - name: resource_group_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the resource group. The name is case insensitive. Required.
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PagedApplicationDefinitionFlattened'
      x-stackql-resource: application_definitions
      x-stackql-method: list_by_resource_group
      x-stackql-verb: SELECT
      x-stackql-objectKey: $.value
      x-stackql-flatten: list
      x-stackql-flattenKey: value
  ? /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/applications/{application_name}?api-version=2019-07-01
  : get:
      operationId: Applications_get
      description: Gets the managed application.
      parameters:
      - name: resource_group_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the resource group. The name is case insensitive. Required.
      - name: application_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the managed application. Required.
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApplicationFlattened'
      x-stackql-resource: applications
      x-stackql-method: get
      x-stackql-verb: SELECT
      x-stackql-flatten: singleton
    put:
      operationId: Applications_begin_create_or_update
      description: Creates a new managed application.
      parameters:
      - name: resource_group_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the resource group. The name is case insensitive. Required.
      - name: application_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the managed application. Required.
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Resource location.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                managedBy:
                  type: string
                  description: ID of the resource that manages this resource.
                sku:
                  $ref: '#/components/schemas/Sku'
                  description: The SKU of the resource.
                plan:
                  $ref: '#/components/schemas/Plan'
                  description: The plan information.
                kind:
                  type: string
                  description: The kind of the managed application. Allowed values are MarketPlace and ServiceCatalog.
                    Required.
                identity:
                  $ref: '#/components/schemas/Identity'
                  description: The identity of the resource.
                properties:
                  type: object
                  properties:
                    managedResourceGroupId:
                      type: string
                      description: The managed resource group Id.
                    applicationDefinitionId:
                      type: string
                      description: The fully qualified path of managed application definition Id.
                    parameters:
                      type: object
                      properties: {}
                      description: Name and value pairs that define the managed application parameters. It can be
                        a JObject or a well formed JSON string.
                    jitAccessPolicy:
                      $ref: '#/components/schemas/ApplicationJitAccessPolicy'
                      description: The managed application Jit access policy.
              required:
              - kind
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Application'
      x-stackql-resource: applications
      x-stackql-method: create_or_update
      x-stackql-verb: INSERT
    delete:
      operationId: Applications_begin_delete
      description: Deletes the managed application.
      parameters:
      - name: resource_group_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the resource group. The name is case insensitive. Required.
      - name: application_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the managed application. Required.
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Success (no content / accepted)
      x-stackql-resource: applications
      x-stackql-method: delete
      x-stackql-verb: DELETE
    patch:
      operationId: Applications_update
      description: Updates an existing managed application. The only value that can be updated via PATCH currently
        is the tags.
      parameters:
      - name: resource_group_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the resource group. The name is case insensitive. Required.
      - name: application_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the managed application. Required.
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Resource location.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                managedBy:
                  type: string
                  description: ID of the resource that manages this resource.
                sku:
                  $ref: '#/components/schemas/Sku'
                  description: The SKU of the resource.
                plan:
                  $ref: '#/components/schemas/PlanPatchable'
                  description: The plan information.
                kind:
                  type: string
                  description: The kind of the managed application. Allowed values are MarketPlace and ServiceCatalog.
                identity:
                  $ref: '#/components/schemas/Identity'
                  description: The identity of the resource.
                properties:
                  type: object
                  properties:
                    managedResourceGroupId:
                      type: string
                      description: The managed resource group Id.
                    applicationDefinitionId:
                      type: string
                      description: The fully qualified path of managed application definition Id.
                    parameters:
                      type: object
                      properties: {}
                      description: Name and value pairs that define the managed application parameters. It can be
                        a JObject or a well formed JSON string.
                    jitAccessPolicy:
                      $ref: '#/components/schemas/ApplicationJitAccessPolicy'
                      description: The managed application Jit access policy.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Application'
      x-stackql-resource: applications
      x-stackql-method: update
      x-stackql-verb: UPDATE
  ? /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/applications?api-version=2019-07-01
  : get:
      operationId: Applications_list_by_resource_group
      description: Gets all the applications within a resource group.
      parameters:
      - name: resource_group_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the resource group. The name is case insensitive. Required.
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PagedApplicationFlattened'
      x-stackql-resource: applications
      x-stackql-method: list_by_resource_group
      x-stackql-verb: SELECT
      x-stackql-objectKey: $.value
      x-stackql-flatten: list
      x-stackql-flattenKey: value
  /subscriptions/{subscription_id}/providers/Microsoft.Solutions/applications?api-version=2019-07-01:
    get:
      operationId: Applications_list_by_subscription
      description: Gets all the applications within a subscription.
      parameters:
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PagedApplicationFlattened'
      x-stackql-resource: applications
      x-stackql-method: list_by_subscription
      x-stackql-verb: SELECT
      x-stackql-objectKey: $.value
      x-stackql-flatten: list
      x-stackql-flattenKey: value
  /{application_id}?api-version=2019-07-01:
    get:
      operationId: Applications_get_by_id
      description: Gets the managed application.
      parameters:
      - name: application_id
        in: path
        required: true
        schema:
          type: string
        description: The fully qualified ID of the managed application, including the managed application name and
          the managed application resource type. Use the format, /subscriptions/{guid}/resourceGroups/{resource-group-name}/Microsoft.Solutions/applications/{application-name}.
          Required.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApplicationFlattened'
      x-stackql-resource: applications
      x-stackql-method: get_by_id
      x-stackql-verb: SELECT
      x-stackql-flatten: singleton
    delete:
      operationId: Applications_begin_delete_by_id
      description: Deletes the managed application.
      parameters:
      - name: application_id
        in: path
        required: true
        schema:
          type: string
        description: The fully qualified ID of the managed application, including the managed application name and
          the managed application resource type. Use the format, /subscriptions/{guid}/resourceGroups/{resource-group-name}/Microsoft.Solutions/applications/{application-name}.
          Required.
      responses:
        '204':
          description: Success (no content / accepted)
      x-stackql-resource: applications
      x-stackql-method: delete_by_id
      x-stackql-verb: DELETE
    put:
      operationId: Applications_begin_create_or_update_by_id
      description: Creates a new managed application.
      parameters:
      - name: application_id
        in: path
        required: true
        schema:
          type: string
        description: The fully qualified ID of the managed application, including the managed application name and
          the managed application resource type. Use the format, /subscriptions/{guid}/resourceGroups/{resource-group-name}/Microsoft.Solutions/applications/{application-name}.
          Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Resource location.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                managedBy:
                  type: string
                  description: ID of the resource that manages this resource.
                sku:
                  $ref: '#/components/schemas/Sku'
                  description: The SKU of the resource.
                plan:
                  $ref: '#/components/schemas/Plan'
                  description: The plan information.
                kind:
                  type: string
                  description: The kind of the managed application. Allowed values are MarketPlace and ServiceCatalog.
                    Required.
                identity:
                  $ref: '#/components/schemas/Identity'
                  description: The identity of the resource.
                properties:
                  type: object
                  properties:
                    managedResourceGroupId:
                      type: string
                      description: The managed resource group Id.
                    applicationDefinitionId:
                      type: string
                      description: The fully qualified path of managed application definition Id.
                    parameters:
                      type: object
                      properties: {}
                      description: Name and value pairs that define the managed application parameters. It can be
                        a JObject or a well formed JSON string.
                    jitAccessPolicy:
                      $ref: '#/components/schemas/ApplicationJitAccessPolicy'
                      description: The managed application Jit access policy.
              required:
              - kind
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Application'
      x-stackql-resource: applications
      x-stackql-method: create_or_update_by_id
      x-stackql-verb: INSERT
    patch:
      operationId: Applications_update_by_id
      description: Updates an existing managed application. The only value that can be updated via PATCH currently
        is the tags.
      parameters:
      - name: application_id
        in: path
        required: true
        schema:
          type: string
        description: The fully qualified ID of the managed application, including the managed application name and
          the managed application resource type. Use the format, /subscriptions/{guid}/resourceGroups/{resource-group-name}/Microsoft.Solutions/applications/{application-name}.
          Required.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Resource location.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                managedBy:
                  type: string
                  description: ID of the resource that manages this resource.
                sku:
                  $ref: '#/components/schemas/Sku'
                  description: The SKU of the resource.
                plan:
                  $ref: '#/components/schemas/Plan'
                  description: The plan information.
                kind:
                  type: string
                  description: The kind of the managed application. Allowed values are MarketPlace and ServiceCatalog.
                    Required.
                identity:
                  $ref: '#/components/schemas/Identity'
                  description: The identity of the resource.
                properties:
                  type: object
                  properties:
                    managedResourceGroupId:
                      type: string
                      description: The managed resource group Id.
                    applicationDefinitionId:
                      type: string
                      description: The fully qualified path of managed application definition Id.
                    parameters:
                      type: object
                      properties: {}
                      description: Name and value pairs that define the managed application parameters. It can be
                        a JObject or a well formed JSON string.
                    jitAccessPolicy:
                      $ref: '#/components/schemas/ApplicationJitAccessPolicy'
                      description: The managed application Jit access policy.
              required:
              - kind
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Application'
      x-stackql-resource: applications
      x-stackql-method: update_by_id
      x-stackql-verb: UPDATE
  ? /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/applications/{application_name}/refreshPermissions?api-version=2019-07-01
  : post:
      operationId: Applications_begin_refresh_permissions
      description: Refresh Permissions for application.
      parameters:
      - name: resource_group_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the resource group. The name is case insensitive. Required.
      - name: application_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the managed application. Required.
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Success (no content / accepted)
      x-stackql-resource: applications
      x-stackql-method: refresh_permissions
      x-stackql-verb: EXEC
  ? /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/jitRequests/{jit_request_name}?api-version=2019-07-01
  : get:
      operationId: JitRequests_get
      description: Gets the JIT request.
      parameters:
      - name: resource_group_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the resource group. The name is case insensitive. Required.
      - name: jit_request_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the JIT request. Required.
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JitRequestDefinitionFlattened'
      x-stackql-resource: jit_requests
      x-stackql-method: get
      x-stackql-verb: SELECT
      x-stackql-flatten: singleton
    put:
      operationId: JitRequests_begin_create_or_update
      description: Creates or updates the JIT request.
      parameters:
      - name: resource_group_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the resource group. The name is case insensitive. Required.
      - name: jit_request_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the JIT request. Required.
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                location:
                  type: string
                  description: Resource location.
                tags:
                  type: object
                  properties: {}
                  description: Resource tags.
                properties:
                  type: object
                  properties:
                    applicationResourceId:
                      type: string
                      description: The parent application id.
                    jitAuthorizationPolicies:
                      type: array
                      items:
                        $ref: '#/components/schemas/JitAuthorizationPolicies'
                      description: The JIT authorization policies.
                    jitSchedulingPolicy:
                      $ref: '#/components/schemas/JitSchedulingPolicy'
                      description: The JIT request properties.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JitRequestDefinition'
      x-stackql-resource: jit_requests
      x-stackql-method: create_or_update
      x-stackql-verb: INSERT
    patch:
      operationId: JitRequests_update
      description: Updates the JIT request.
      parameters:
      - name: resource_group_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the resource group. The name is case insensitive. Required.
      - name: jit_request_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the JIT request. Required.
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tags:
                  type: object
                  properties: {}
                  description: Jit request tags.
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JitRequestDefinition'
      x-stackql-resource: jit_requests
      x-stackql-method: update
      x-stackql-verb: UPDATE
    delete:
      operationId: JitRequests_delete
      description: Deletes the JIT request.
      parameters:
      - name: resource_group_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the resource group. The name is case insensitive. Required.
      - name: jit_request_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the JIT request. Required.
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '204':
          description: Success (no content / accepted)
      x-stackql-resource: jit_requests
      x-stackql-method: delete
      x-stackql-verb: DELETE
  /subscriptions/{subscription_id}/providers/Microsoft.Solutions/jitRequests?api-version=2019-07-01:
    get:
      operationId: JitRequests_list_by_subscription
      description: Retrieves all JIT requests within the subscription.
      parameters:
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JitRequestDefinitionListResultFlattened'
      x-stackql-resource: jit_requests
      x-stackql-method: list_by_subscription
      x-stackql-verb: SELECT
      x-stackql-objectKey: $.value
      x-stackql-flatten: list
      x-stackql-flattenKey: value
  ? /subscriptions/{subscription_id}/resourceGroups/{resource_group_name}/providers/Microsoft.Solutions/jitRequests?api-version=2019-07-01
  : get:
      operationId: JitRequests_list_by_resource_group
      description: Retrieves all JIT requests within the resource group.
      parameters:
      - name: resource_group_name
        in: path
        required: true
        schema:
          type: string
        description: The name of the resource group. The name is case insensitive. Required.
      - name: subscription_id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JitRequestDefinitionListResultFlattened'
      x-stackql-resource: jit_requests
      x-stackql-method: list_by_resource_group
      x-stackql-verb: SELECT
      x-stackql-objectKey: $.value
      x-stackql-flatten: list
      x-stackql-flattenKey: value
  /providers/Microsoft.Solutions/operations?api-version=2019-07-01:
    get:
      operationId: _ApplicationClient_list_operations
      description: Lists all of the available Microsoft.Solutions REST API operations.
      parameters: []
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PagedOperation'
      x-stackql-resource: operations
      x-stackql-method: list_operations
      x-stackql-verb: SELECT
      x-stackql-objectKey: $.value
